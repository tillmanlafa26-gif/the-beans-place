import { AnimatePresence, motion } from "framer-motion";
import { Minus, Plus, ShoppingBag, X } from "lucide-react";
import Button from "./ui/Button";

export default function CartSidebar({ items, open, onClose, onUpdateQuantity }) {
  const subtotal = items.reduce((total, item) => total + Number(item.price.replace("$", "")) * item.quantity, 0);
  return <AnimatePresence>{open && <><motion.button className="cart-backdrop" aria-label="Close shopping cart" onClick={onClose} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} /><motion.aside className="cart-sidebar" aria-label="Shopping cart" initial={{ x: "100%" }} animate={{ x: 0 }} exit={{ x: "100%" }} transition={{ type: "spring", damping: 28, stiffness: 260 }}><div className="cart-header"><div><p className="cart-kicker">Your selection</p><h2>Shopping cart</h2></div><button className="icon-button" onClick={onClose} aria-label="Close shopping cart"><X size={22} /></button></div><div className="cart-items">{items.length === 0 ? <div className="cart-empty"><ShoppingBag size={42} strokeWidth={1.2} /><h3>Your cart is waiting.</h3><p>Choose a coffee and make it yours.</p></div> : items.map((item) => <div className="cart-item" key={item.name}><img src={item.image} alt="" /><div className="cart-item-details"><div className="cart-item-title"><h3>{item.name}</h3><strong>{item.price}</strong></div><p>{item.origin} · {item.roast} Roast</p><div className="quantity-control"><button onClick={() => onUpdateQuantity(item.name, -1)} aria-label={`Decrease ${item.name} quantity`}><Minus size={14} /></button><span>{item.quantity}</span><button onClick={() => onUpdateQuantity(item.name, 1)} aria-label={`Increase ${item.name} quantity`}><Plus size={14} /></button></div></div></div>)}</div><div className="cart-footer"><div className="cart-total"><span>Subtotal</span><strong>${subtotal.toFixed(2)}</strong></div><Button variant="accent" size="lg" className="w-full" disabled={!items.length}>Checkout</Button><small>Shipping calculated at checkout.</small></div></motion.aside></>}</AnimatePresence>;
}
